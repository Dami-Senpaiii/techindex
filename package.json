{
  "name": "techindex-static",
  "version": "1.0.0",
  "private": true,
  "description": "Static TechIndex landing page served via Cloudflare Pages.",
  "scripts": {
    "build": "echo 'Static site: no build step required.'"
  }
}
